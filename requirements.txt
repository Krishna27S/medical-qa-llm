# =============================================================================
# Medical Q&A LLM Fine-Tuning — Dependencies
# =============================================================================
# Tested on Google Colab (Python 3.10, CUDA 12.x, T4 GPU)
#
# WHY pin minimum versions:
#   - transformers >=4.45.0: Mistral v0.3 support + SFTTrainer compatibility
#   - peft >=0.13.0: "all-linear" target_modules shorthand, kbit training fixes
#   - trl >=0.12.0: SFTTrainer API stability, packing support
#   - bitsandbytes >=0.44.0: stable 4-bit NF4 quantization on CUDA 12.x
#   - accelerate >=1.0.0: required by transformers for device_map="auto"
#
# INSTALL:
#   pip install -r requirements.txt
# =============================================================================

# --- Core LLM Libraries ---
torch>=2.1.0
transformers>=4.45.0
datasets>=3.0.0
peft>=0.13.0
trl>=0.12.0
bitsandbytes>=0.44.0
accelerate>=1.0.0

# --- Evaluation ---
evaluate>=0.4.0
rouge-score>=0.1.2
nltk>=3.8.0

# --- Deployment ---
gradio>=5.0.0

# --- Visualization & Utilities ---
matplotlib>=3.7.0
pyyaml>=6.0
scipy>=1.10.0
